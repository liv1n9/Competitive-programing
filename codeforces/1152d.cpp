#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

const ll MOD = 1e9 + 7;
const int N = 1008;

ll f[N * 2][2][2];
ll g[N][N];
int n;

int main() {
    cin >> n;
    g[0][0] = 1;
    for (int i = 1; i <= n * 2; i++) {
        for (int x = i / 2; x < i; x++) {
            
        }
    }
}
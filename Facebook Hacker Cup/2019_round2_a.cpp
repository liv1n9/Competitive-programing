#include <bits/stdc++.h>
using namespace std;

int main() {
    int test;
    cin >> test;
    for (int I = 1; I <= test; I++) {
        int n, m, k;
        cin >> n >> m >> k;
        vector<int> x(k), y(k);
        for (int i = 0; i < k; i++) {
            cin >> x[i] >> y[i];
        }
        cout << "Case #" << I << ": ";
        if (n == 1) {
            cout << "N\n";
        } else {
            
        }
    }
}